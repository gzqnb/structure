1 先定义一个节点类（类里存放data和指针next）
2 再定义一个单向链表类（初始化为一个头结点，不存放具体数据，头结点初始化时next为null）
3 注意单向链表的各种方法在遍历链表时，都要有一个tmp来存储节点，随后进行循环来遍历
4 注意在按顺序插入、删除和更新时，都可以设置一个标志变量，来判断是否找到了该节点，
如果找到了设置flag标志为true，接下来进行相应的逻辑
5 顺序插入逻辑找到的节点都是tmp.next，是找到该节点的下一个，这样才方便插入